import React from "react";
import "../styles/OvertimeRequest.css";

const BodyContent = () => {
    return (
        <div className="over-req">
            <div className="body-content-container">
                <p>Hello Overtime Request SubModule!</p>
                <p>Fill this container with your elements, change the display if need be.</p>
                <p>If you're going to style with css, use your unique namespace '.over-req' at the start.</p>
            </div>
        </div>
    );
};

export default BodyContent;